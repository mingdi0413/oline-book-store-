<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>도서 판매 프로그램</title>
  </head>
  <body>
    <h1>
      <div style="text-align: center">Oline Book Store</div>
    </h1>
    <section id="read-form">
      <h2><div style="text-align: center">회원님의 쿠폰목록</div></h2>
      <table id="read-table">
        <thead>
          <tr id="field">
            <th>쿠폰번호</th>
            <th>쿠폰 내용</th>
            <th>사용 유무</th>
          </tr>
        </thead>
        <input
          type="button"
          value="메인 화면"
          onClick="location.href='/book/book-main'"
        />

        <tbody>
          <% for(let i = 0; i < result.length; i++){%>
          <tr>
            <td><%=result[i].id%></td>
            <td><%=result[i].content%></td>
            <td><%=result[i].useYN%></td>
          </tr>
          <%}%>
        </tbody>
      </table>
    </section>
  </body>
</html>
