<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>도서 판매 프로그램</title>
  </head>
  <body>
    <h1>
      <div style="text-align: center">Oline Book Store</div>
    </h1>
    <section id="read-form">
      <h2><div style="text-align: center">회원님의 주문내역</div></h2>
      <input
        type="button"
        value="메인 화면"
        onClick="location.href='/book/book-main'"
      />
      <input
        type="button"
        value="주문 수정"
        onClick="location.href='/order/order_refund'"
      />
      <table id="read-table">
        <thead>
          <tr id="field">
            <th>주문 날짜</th>
            <th>주문 총액</th>
            <th>배송지</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i = 0; i < result.length; i++){%>
          <tr>
            <td><%=result[i].order_date%></td>

            <td><%=result[i].order_total%></td>

            <td><%=result[i].order_default_address%></td>
            <td>
              <input
                type="button"
                value="상세정보 조회"
                onClick="location.href='/order/order_detail?order_num=<%=result[i].order_num%>'"
              />
            </td>

            <%}%>
          </tr>
        </tbody>
      </table>
    </section>
  </body>
</html>
