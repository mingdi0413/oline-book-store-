<!DOCTYPE html>
<html lang="ko">
  <head>
    <link href="/static/css/reset.css" rel="stylesheet" type="text/css">
    <link href="/static/css/addorder.css" rel="stylesheet" type="text/css">
    <title>도서 판매 프로그램</title>
  </head>

  <body>
    <section id="addorder-form">
      <h1>주문하기</h1>
      <form method="post" action="./order" class="order-wrap">
        <table>
          <label>우편번호</label>
          <select name="order_zip_code">
            <% for(let i = 0; i < address.length; i++){%>
            <option value="<%=address[i].zip_code%>"><%=address[i].zip_code%></option>
            <%}%>
          </select>
          <label>카드 번호</label>
          <select name="card_id">
            <% for(let i = 0; i < card.length; i++){%>
            <option value="<%=card[i].card_id%>"><%=card[i].card_id%></option>
            <%}%>
          </select>
          <label>사용하실 포인트</label>
          <input
            type="text"
            class="input"
            placeholder="포인트"
            id="minusPoint"
            name="minusPoint"
          />
          <label>사용하실 쿠폰</label>
          <select name="own_coupon_num">
            <option value="0">사용안함</option>
            <% for(let i = 0; i < result.length; i++){%>
            <option value="<%=result[i].num%>"><%=result[i].name%></option>
            <%}%>
          </select>
          
        </table>
        <input type="submit" value="주문하기"  class="orderBtn"/>
      </form>
       
</html>
